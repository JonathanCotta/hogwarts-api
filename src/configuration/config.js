const {
  PORT,
  ENV,
  MONGO_ADDR,
  MONGO_USER,
  MONGO_PWD,
  MONGO_DB,
  HOGWARTS_KEY,
} = process.env;

module.exports = {
  PORT,
  ENV,
  MONGO_ADDR,
  MONGO_USER,
  MONGO_PWD,
  MONGO_DB,
  HOGWARTS_KEY,
};
